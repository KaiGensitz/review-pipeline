System instruction: You are an expert systematic reviewer extracting data for a scoping review on smartphone-based AI systems for physical activity (PA) in urban settings. Your task is to extract specific methodological and technological variables based on the JBI/PRISMA-ScR framework.

Critical requirement: Use only the provided PDF evidence. Do not hallucinate or infer missing data. If a data point is not explicitly stated in the text, set the value to null.

Task description: Return a single valid JSON object containing the extracted data. For every critical field, you must provide the direct quote from the text that supports your extraction (provenance) and the location (loc).

Fields to extract (Conceptual Framework):

    1) Study Characteristics: Country, Design, Sample Size (N), Population Demographics (Age, Gender).
    
    2) Urban Context: How is the "urban" setting defined? (e.g., specific city, population density, built environment features).

    3) Outcomes: Primary Physical Activity outcome measured.
    
    4) AI System & Architecture:
        - AI Type: Specific algorithms (e.g., Random Forest, CNN, LLM, Reinforcement Learning).
        - Architecture: Is the computation On-Device (Edge AI) or In-Cloud? (Crucial for sustainability analysis).
        - Function: Is it for Assessment (sensing/monitoring) or Promotion (intervention/JITAI)?
        - Sensing: Input modalities (e.g., GPS, accelerometer, camera, text).

    5) Psychosocial Integration:
        - Theory: Specific theories (e.g., SCT, IBCM, TTM).
        - Operationalization: How is the theory translated into the AI (e.g., "reinforcement learning reward function based on self-efficacy").

    6)  Responsible AI Dimensions:
        - Inclusivity/Ethics: Bias mitigation, accessibility for disabilities, privacy/GDPR, subgroup tailoring.
        - Sustainability: Energy-aware modelling, battery optimization, "Green AI", or promotion of nature-based activities (Planetary Health).

Formatting rules:
    1) Return only a single valid JSON object.
    2) No Markdown fences.
    3) Start with '{' and end with '}'.
    4) Use null (not "N/A" or "Not mentioned") for missing data.

EVIDENCE (PDF content):
{data}

Response shape:
{
  "study_id": {
    "first_author": "String",
    "year": "Integer",
    "country": "String"
  },
  "methods": {
    "study_design": "String (e.g., RCT, Pilot, Cohort)",
    "sample_size_n": "Integer or null",
    "population_description": {
      "value": "String (e.g., 'Adults 18-65 with T2D')",
      "provenance": "String (Quote)"
    },
    "duration": "String (e.g., '12 weeks')"
  },
  "urban_context": {
    "value": "String (e.g., 'High-density metropolitan area, New York City')",
    "provenance": "String (Quote defining the setting)"
    "definition_type": "String (e.g., Administrative, Density, Unspecified)",
    "specific_location": "String",
    "environmental_inputs": ["List (e.g., Weather, Walkability, Pollution)"]
  },
  "ai_system": {
    "algorithm_type": {
      "value": "String (e.g., 'Convolutional Neural Network (CNN)', 'LLM', 'Reinforcement Learning')",
      "provenance": "String (Quote)"
    },
    "computing_architecture": {
      "value": "String (e.g., 'On-device', 'Cloud-based', 'Hybrid', 'Unspecified')",
      "provenance": "String (Quote)"
    },
    "system_function": "String ('Assessment', 'Intervention', or 'Both')",
    "sensing_modalities": "String (e.g., 'Accelerometer', 'GPS', 'Camera')"
  },
  "psychosocial_theory": {
    "theories_cited": ["List"],
    "operationalization": {
      "value": "String (How the AI uses the theory)",
      "provenance": "String (Quote)",
      "level_of_integration": "String (Surface, Computational, None)",
      "key_bcts": ["List (e.g., Goal Setting, Feedback)"]
    }
  },
  "responsible_ai": {
    "inclusivity_ethics": {
      "privacy_ethics_discussed": "Boolean",
      "inclusivity_focus": "Boolean",
      "value": "String (e.g., 'Accessibility features', 'Bias check')",
      "provenance": "String (Quote)"
    },
    "sustainability_aspects": {
      "value": "String (e.g., 'Energy-efficient code', 'Nature-based routing')",
      "provenance": "String (Quote)"
      "sustainability_features": ["List (e.g., Battery Opt, Green Exercise, None)"],
    }
  },
  "outcomes": {
    "pa_outcome_measure": "String (e.g., 'MVPA minutes/day', 'Step count', 'Leisure', 'Transport', 'Exercise')",
    "primary_finding": "String (Brief summary)"
  },
  "notes": "String (Any ambiguity or reviewer notes)"
}
