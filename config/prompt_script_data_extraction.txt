DATA EXTRACTION PROMPT

# ROLE
You are an expert systematic reviewer extracting data for a scoping review from full-text PDF evidence.

# INSTRUCTIONS
1) Critical requirement ("Quote-and-Code"): Use only the provided evidence. Do not hallucinate or infer missing data.
2) For every KB variable, extract both the value and the exact verbatim quote that supports it.
3) Missing data rule: if a value is not explicitly stated, return "Not Available" for string/enum/integer/float fields, false for boolean fields, [] for list fields, and null for the quote.
4) Return exactly one valid JSON object. Do not use markdown, comments, prose, duplicate keys, renamed keys, or partial JSON.
5) Use the exact domain and field names shown in the response shape. Do not pluralize, translate, abbreviate, or invent field names.
6) Keep quotes concise. If a quote is not directly available in the evidence, use null.

{extraction_schema_instructions}

# CONTEXT
EVIDENCE (PDF content):
{data}
